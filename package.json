{
    "name": "@wbk--reactjs-forge/core",
    "version": "0.0.0",
    "main": "index.js",
    "author": "paulAlexSerban <paul.alex.serban@gmail.com>",
    "license": "MIT",
    "scripts": {
        "build:apps:dev": "rm -rfv .parcel-cache && lerna run build:dev",
        "process:assets-media": "lerna run process",
        "////": "CI PRODUCTION BUILDS",
        "build:full": "yarn build:apps:prod && yarn package:apps && yarn dashboard:deps && yarn build:dashboard:prod && yarn package:dashboard",
        "build:apps:prod": "rm -rfv .parcel-cache && rm -rfv ./frontend/apps/*/dist && lerna run build:prod",
        "package:apps": "rm -rfv ./package/* && bash scripts/package-apps.bash ",
        "dashboard:deps": "node scripts/dashboard-deps.js",
        "build:dashboard:prod": "lerna run build:dashboard:prod",
        "package:dashboard": "bash scripts/package-dashboard.bash",
        "///": "LOCAL SERVER",
        "start:package": "npx serve package",
        "//": "TOOLS",
        "formats:check": "yarn prettier --check .",
        "formats:write": "yarn prettier --write .",
        "lint": "lerna run lint --concurrency=5",
        "test": "yarn lerna run test --stream --parallel",
        "/": "GIT & VERSIONING",
        "commit": "yarn git-cz",
        "version:modules": "bash scripts/version.bash",
        "prepare": "husky install"
    },
    "devDependencies": {
        "@commitlint/cli": "^21.0.2",
        "@commitlint/config-conventional": "^21.0.2",
        "commitizen": "^4.3.0",
        "cz-conventional-changelog": "^3.3.0",
        "husky": "^9.1.7",
        "lerna": "^9.0.7",
        "prettier": "^3.8.3"
    },
    "dependencies": {
        "@semantic-release/changelog": "^6.0.3",
        "@semantic-release/commit-analyzer": "^13.0.1",
        "@semantic-release/git": "^10.0.1",
        "@semantic-release/github": "^12.0.8",
        "@semantic-release/npm": "^13.1.5",
        "@semantic-release/release-notes-generator": "^14.1.1",
        "semantic-release": "^25.0.3"
    },
    "keywords": [],
    "private": true,
    "workspaces": {
        "packages": [
            "assets/*",
            "assets/",
            "backend/*",
            "backend/**/*",
            "database/*",
            "frontend/*",
            "frontend/**/*",
            "shared/*",
            "shared/**/*",
            "scripts/*",
            "dashboard/*"
        ],
        "nohoist": [
            "**/normalize-scss"
        ]
    },
    "config": {
        "commitizen": {
            "path": "./node_modules/cz-conventional-changelog"
        }
    },
    "husky": {
        "hooks": {
            "prepare-commit-msg": "exec < /dev/tty && npx cz --hook || true",
            "commit-msg": "commitlint -E HUSKY_GIT_PARAMS"
        }
    }
}
